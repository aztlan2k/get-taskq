
var expect = require('chai').expect;

describe('get-task should', function() {
  before(function() {
    console.log('setting up...')
  });

  it('setup view', function() {
    expect(false).to.be.true
  });
});
